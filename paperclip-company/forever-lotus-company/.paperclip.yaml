schema: paperclip/v1
agents:
  ceo:
    inputs:
      env:
        GH_TOKEN:
          kind: secret
          requirement: optional
  cso-standards-policy:
    inputs:
      env:
        GH_TOKEN:
          kind: secret
          requirement: optional
  cro-revenue:
    inputs:
      env:
        GH_TOKEN:
          kind: secret
          requirement: optional
  governance-lead:
    inputs:
      env:
        GH_TOKEN:
          kind: secret
          requirement: optional
  evidence-lead:
    inputs:
      env:
        GH_TOKEN:
          kind: secret
          requirement: optional
  partnerships-lead:
    inputs:
      env:
        GH_TOKEN:
          kind: secret
          requirement: optional
  outreach-pods-lead:
    inputs:
      env:
        GH_TOKEN:
          kind: secret
          requirement: optional
  platform-reliability:
    inputs:
      env:
        GH_TOKEN:
          kind: secret
          requirement: optional
  security-risk:
    inputs:
      env:
        GH_TOKEN:
          kind: secret
          requirement: optional
