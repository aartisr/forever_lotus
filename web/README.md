# Forever Lotus Web
# Legacy Web Notes

This directory reflects the original static-site version of Forever Lotus.

## Current Status

- The active website now lives in the root-level Next.js application under `src/`
- Vercel deployments should target the repository root, not this directory
- The public repository is `https://github.com/aartisr/forever_lotus`

## Why This Folder Remains

This folder is kept as archival context for the project evolution from a standalone static presentation into a future-ready application architecture.

## Active Development

Use the root project commands instead:

```bash
npm install
npm run dev
npm run build
```
